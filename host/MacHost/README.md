# Carlink for macOS
Carlink - CPC200-CCPA App for macOS
Personal app used for ccpa testing. Fork and use as you want, will only update as my needs change.

<!-- CORRECTED 2026-08-16: the previous seven embeds were root-absolute (`/screenshots/...`) AND named
     files that no longer exist (portrait, landscape, media_menubar, call_notification, resolution_list,
     custom_resolution, fullscreen_match — the last of which documented the fullscreen match-resolution
     prompt removed 2026-08-02). These three are what `screenshots/` actually contains. -->

![Main window — CarPlay rendering with the floating control box](screenshots/mainUI.png)
![Settings — Configuration tab (video, codec, alt stream, audio)](screenshots/settings1.png)
<!-- settings2.png (Settings ▸ CCPA tab) is deliberately NOT embedded: it shows the box's real
     Bluetooth MAC, Wi-Fi MAC and serial number. The embed was added 2026-08-16 while repairing the
     broken image links and removed the same day once the contents were actually looked at. The file
     is still tracked in git (added in f3fa61d) — redacting or removing it is a separate decision. -->
*(The Settings ▸ CCPA tab screenshot is omitted — it displays the unit's Bluetooth/Wi-Fi MACs and
serial number.)*
